  <li id="<%= post.id%>">
    <%= post.title %>
    <%= link_to "Show", post_path(post.id), data: { remote: true} %>
    <%= link_to "Edit", edit_post_path(post.id), data: { remote: true} %>
    <%= link_to "Destroy", post_path(post.id), method: :delete, data: { remote: true } %>
  </li>
